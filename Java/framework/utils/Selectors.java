//add imports

public class Selectors {
 // put all login in one UI.Login_selectors ? SideNav wit its own selectors
  private static final By LOGIN_USERNAME = By.id("QA_ID_InputText_login"); //use selenide JQuery

  private static final By LOGIN_PASSWORD = By.id("QA_ID_InputText_password");

}
